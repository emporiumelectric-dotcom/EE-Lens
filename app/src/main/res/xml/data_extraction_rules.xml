<?xml version="1.0" encoding="utf-8"?>
<!--
  EE Lens keeps the shop's product catalogue and photos on this phone only.
  Both transfer paths are refused explicitly so no product data can leave the
  device through Google's cloud backup or a device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
